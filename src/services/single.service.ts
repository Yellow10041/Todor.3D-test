import {instance} from "api/api.interseptor"

export const Single = {
    // async getGalleries() {
    //     const response = await instance<IGalleryResponse>("gallery-list")
    //     return response.data.data.results[0].galleries
    // },
}
